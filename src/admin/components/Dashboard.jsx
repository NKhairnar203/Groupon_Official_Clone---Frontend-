import React from "react";

const Dashboard = () => {
  return (
    <div className="p-6">
      <h2 className="text-2xl font-semibold">Welcome to the Dashboard</h2>
      <p className="mt-4">
        Select a section from the sidebar to manage the data.
      </p>
    </div>
  );
};

export default Dashboard;
